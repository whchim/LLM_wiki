streamlit>=1.36,<2
pyyaml>=6.0
pandas>=2.0
psycopg[binary]>=3.1
psycopg_pool>=3.1
fastapi>=0.115
uvicorn[standard]>=0.30
PyJWT>=2.8
pwdlib[argon2]>=0.2
# 可选依赖：LLM 层可观测性探针（SP2.5）——未配置 LANGFUSE_* 环境变量时无需安装
# langfuse>=2.10
