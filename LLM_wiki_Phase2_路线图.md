# LLM Wiki 知识库平台 Phase 2 路线图

> **版本**：v0.1 ｜ **日期**：2026-08-19 ｜ **状态**：草案（待评审）
>
> **定位**：Phase 2「企业级能力」的总体规划文档——子项目拆分、迭代排期、架构决策、待定决策、退出标准。每个子项目（SP）的详细设计另走「设计文档 → 实施计划」流程，不在本文档展开。
>
> **依据**：[LLM_wiki_PRD.md](LLM_wiki_PRD.md) v1.7（需求唯一来源，本文档落地后 PRD 升 v1.8 同步细化）；[LLM_wiki_设计文档.md](LLM_wiki_设计文档.md) v0.1（Demo 设计基线）。

---

## 一、定位与范围

Phase 2 的本质：从「单用户 Demo 验证」过渡到「多用户企业可用」。以 PRD 第七章 Phase 2 表为基础，按必做 / 可选切割：

| 簇 | 模块 | 优先级 |
|---|---|---|
| 地基 | PostgreSQL 迁移、FastAPI 后端、JWT 认证 / 角色 | **必做** |
| 检索 | 混合检索（grep + 向量 + re-rank）、增量编译、断点续跑 | **必做** |
| 智能 | 健康巡检 + 周报、知识演进、关联涌现 | **必做** |
| 审计 | 审计日志（audit_logs） | **必做**（并入 SP2） |
| 整理 | 目录按部门分类、done/ 归档清理 | **必做**（并入 SP5） |
| 治理/体验 | 多源同步、React 管理后台 | **划入 Phase 3**（本阶段不做） |

**明确不做（划入 Phase 3）**：多源同步、React 前端重写、知识图谱、外部源感知、完整 RBAC、分布式编译、审计闭环、生产部署、性能优化（10 万+ 条目 / 100 并发）。

## 二、已拍板的架构决策

1. **Agent 宿主边界（Claude Code 掌 LLM，API 掌数据）**
   Claude Code + Harness 仍是编译/审核/问答的 LLM 引擎；FastAPI 接管的是**数据访问层**——替代 Bash 直连 SQLite/文件，提供 REST API（上传/审核/搜索/管理）、JWT 权限、检索 API、向量库。不重造 LLM 调用与 Agent 编排（PRD 架构原则）。
2. **向量存储选型（PostgreSQL + pgvector）**
   随 SQLite→PostgreSQL 迁移，向量检索直接用 pgvector 扩展，不引入 ChromaDB：减少一个容器与运维点、元数据与业务数据同库、Join/过滤一体。带**重评开关**：Phase 3 若进入 10 万+ 条目规模再评估独立向量库。

## 三、子项目拆分（5 个 SP）

### SP1 数据地基 —— PostgreSQL 迁移

- **目标**：SQLite 全量迁移至 PostgreSQL 16 + pgvector 扩展；4 张现有表（knowledge_entries / compile_tasks / pending_reviews / search_logs）+ 4 张 Phase 2 新增表（audit_logs / contributors / conflicts / health_reports）；幂等数据迁移脚本；**双写铁律保持**（YAML 仍为规范数据源）。
- **依赖**：无（第一个 SP）。
- **落地决策**：D-双写并发（多用户下 YAML 规范源与 PG 缓存的冲突策略）。
- **退出标准**：迁移脚本可重复执行；索引重建后 YAML ↔ PG 全量一致；Demo 35 个测试转绿；Docker Compose 起 PostgreSQL + 应用。

### SP2 API 与安全 —— FastAPI + JWT + 审计

- **目标**：FastAPI 应用容器；REST API 化上传/审核/搜索/管理端点（替代 Streamlit 直连文件系统 + SQLite）；JWT 登录 + 管理员/普通用户角色区分；审计日志中间件（写操作事件落 audit_logs 表）。
- **依赖**：SP1。
- **落地决策**：D-前端策略（迭代 1-3 保留 Streamlit，接入 JWT 登录）、D-多用户写入（普通用户上传是否直接走 API 的权限模型）。
- **退出标准**：全部管理操作可经 API 完成；JWT 鉴权中间件生效，越权访问被拒；审计日志记录核心写操作；Streamlit 消费 API 或带权限校验。

### SP3 增量编译 —— file watcher + 断点续跑

- **目标**：file watcher 监听 RAW 目录变更自动触发增量编译；compile_tasks 状态机完善（pending/processing/done/failed/cached + 断点恢复，不重复编译）；触发方式从纯手动触发文件扩展为 watcher + API/CLI 触发。
- **依赖**：SP1（compile_tasks 表）、SP2（触发端点）。
- **退出标准**：单次增量编译 < 30 秒（RAW ≤ 100 份、单文档 < 5 万字符，PRD 非功能需求）；中断后续跑不重复执行已完成任务。

### SP4 混合检索 —— grep + 向量 + re-rank

- **目标**：embedding 服务；pgvector 向量列 + HNSW 索引；编译/审核入库时同步写向量；双通道检索（grep 精确匹配 + 向量语义检索）+ 权重融合 re-rank（初始 0.5/0.3/0.2，实测调优）；`/ask` 与 Streamlit 搜索改走新链路；**目标 P99 < 3 秒 @ 1 万条目**。
  - **向量 = 可从 YAML 重建的缓存**（沿用「SQLite 只是缓存」原则）：模型换版/索引损坏仅重算全部条目，无数据丢失风险，不做权威存储。
  - **grep 为内建降级通道**：embedding 服务故障时检索自动退化为 Demo 的 grep-only 行为（仅失去模糊匹配，不崩）。
  - **未命中语义重定义**：混合检索后向量通道对模糊查询也会返回结果，`match_count = 0` 不再是唯一「未命中」判据——需定义新的缺口判据，与 SP5 的缺口聚合/周报同步适配。
- **依赖**：SP1（pgvector）、SP2(检索 API)、SP3（增量入库触发向量构建）。
- **退出标准**：模糊描述/概念类查询命中率对照用例通过；P99 < 3 秒 @ 1 万条目；grep 精确匹配通道行为不回退；embedding 故障自动降级 grep-only 且服务不崩；「未命中」判据重定义完成并联动 SP5 缺口聚合。

### SP5 知识智能（Claude Code 轨）—— 巡检 / 演进 / 涌现

- **目标**：
  - 健康巡检：孤立节点、wikilink 断链、过期（>180 天未更新 → stale）、语义冲突检测；产出 health_reports 周报（含增长趋势）。
  - 知识演进：去重相似检测（70-90% 相似）→ 更新/修正建议 → 审核后版本自增（V1.0 → V1.1）；概念被更新后引用方标记「待核实」。
  - 关联涌现：3+ 概念共引同一术语但无独立条目 → 建议新建；2 概念高度重叠 → 建议合并；零引用孤立 → 建议归档。
  - 缺口聚合适配：自增长引擎的「未命中」判据按 SP4 重定义结果切换（search_logs 的缺口 Top 20 / 周报改按新判据聚合，避免向量通道削弱缺口感知）。
  - 顺带整理：目录按部门分类迁移、done/ 归档清理策略。
- **依赖**：SP1（conflicts/contributors/health_reports 表）、既有编译/审核 Claude Code 工作流、全量语料。**不依赖** SP2-SP4（可并行）。
- **退出标准**：周报自动生成含 5 类指标；版本自增链路走通；合并/拆分/新建建议进入审核流；部门目录分类迁移完成；done/ 不再无限膨胀。

## 四、迭代计划与依赖

```
依赖图：
SP1 ──┬──→ SP2 ──→ SP3 ──→ SP4
      │               （审计日志并入 SP2）
      └──→ SP5（Claude 轨：仅依赖 schema 与既有审核流，可并行）
              （部门分类/done 归档并入 SP5 尾期）

迭代排期：
迭代 1（2-3 周）  SP1 数据地基           【唯一串行前提】
迭代 2（3-4 周）  SP2 API与安全 ‖ SP5 知识智能【双轨并行启动】
迭代 3（3 周）    SP3 增量编译 + SP4 混合检索
迭代 4（1-2 周）  整合验收 + 文档收尾

合计约 10-12 周（对应 PRD 第七章 Phase 2 的 2-3 个月）
```

**并行关键**：SP5 走 Claude Code 轨，只依赖 SP1 的 schema 与既有审核流，不依赖后端 API/增量编译/向量库——SP1 完成即解锁全场并行，较「地基→检索→智能」纯串行省出接近一整条迭代。

## 五、待定决策（后续 SP 设计阶段再拍）

| 编号 | 决策 | 归属 | 时效 |
|---|---|---|---|
| D-embedding | 中文企业语料 embedding 模型：本地 bge-m3（贴合 Phase 3 私有化）vs 云 API（火山/通义，落地快） | SP4 前置 | **最晚迭代 2 内敲定**，否则卡 SP4 |
| D-前端策略 | 迭代 1-3 保留 Streamlit + JWT 登录，React 已划入 Phase 3 | SP2 | 迭代 2 |
| D-多用户写入 | 普通用户上传直接走 API？审核仍人工在管理界面？触发的编译消费方式？ | SP2 | 迭代 2 |
| D-双写并发 | 多用户下 YAML（规范源）与 PG（缓存）双写的并发冲突策略 | SP1 | 迭代 1 |
| D-重评开关 | pgvector 若在 1 万条规模暴露瓶颈 → 评估独立向量库 | SP4 | Phase 3 正式重评 |

## 六、风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| embedding 选型延误 | 卡 SP4 | 最晚迭代 2 内敲定；选型期可先用 stub 向量（模拟相似度）并行开发 |
| PostgreSQL 迁移破坏既有 35 测试 | 回归 | 迁移是改造非重写；测试套件先行转绿再进新功能 |
| 双轨并行冲突（SP2 改文件写入路径 vs SP5 读写 YAML） | 互相踩 | 明确边界：SP5 只用 YAML + 审核流 + Claude 轨，不碰 API 层 |
| 10 万+ 条目规模提前到来 | 向量与并发瓶颈 | 启用 D-重评开关；粒度上先保证 1 万条达标 |

## 七、阶段退出标准（Phase 2 Done 定义）

- [ ] 多用户可登录（管理员/普通用户），角色权限生效，越权访问被拒
- [ ] 混合检索 P99 < 3 秒 @ 1 万条目，模糊/概念类查询命中；grep 精确匹配不回退
- [ ] 增量编译单次 < 30 秒，支持断点续跑
- [ ] 健康周报自动生成（5 类指标）；演进/涌现建议进入审核流；版本自增链路走通
- [ ] 审计日志覆盖核心写操作
- [ ] 数据全量在 PostgreSQL（SQLite 退役）；YAML ↔ PG 双写一致
- [ ] 全部 Demo 35 测试 + Phase 2 新增测试绿

## 八、文档落地

- 本文档（`LLM_wiki_Phase2_路线图.md`）为 Phase 2 主规划，放项目根，与 PRD/设计文档/实施计划同层。
- PRD 升 v1.8：第七章 Phase 2 表细化到 SP 级，多源同步/React 移入 Phase 3。
- 后续每个 SP 单独走「设计文档 → 实施计划」流程，首个子项目为 **SP1 数据地基**。

---

## Changelog

- **v0.1（2026-08-19）**：初稿。基于 PRD v1.7 第七章 Phase 2 范围，经 brainstorming 决策：必做范围收敛为地基/检索/智能/审计/整理五簇；多源同步与 React 划入 Phase 3；架构决策确定 Claude Code 掌 LLM + FastAPI 掌数据、PostgreSQL + pgvector；SP5 并行起步压缩排期。评审补充：SP4 明确「向量 = 可从 YAML 重建的缓存 + grep 内建降级通道」，SP4/SP5 增加「未命中语义重定义」处理项。